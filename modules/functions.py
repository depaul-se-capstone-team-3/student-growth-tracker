"""This module centralizes queries. Anything that has the potential to be used
more than once should go here, or another module in this directory."""


# ############################################################################
# ## Turn th(is|ese) into a? function(s)?.
# if not class_id:
#     response.flash = T("Class Does Not Exist")
#     session.flash = T("Class does not exist.")

# # Redirect to previous link if via link, else redirect to main page.
# if (request.env.http_referer==None):
#     redirect(URL('default', 'index'))
# else:
#     redirect(request.env.http_referer)
# ##
# ############################################################################
