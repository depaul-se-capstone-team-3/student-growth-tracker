/*
 Functions we need for the sgt application.
*/

// A hack to fix a bug in web2py.
$().ready(function() {
    $(":submit").removeClass("btn-default");
     $("button").removeClass("btn-default");
});
